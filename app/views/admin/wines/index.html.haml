%h2 Vin à editer

%input.string.search-bar{placeholder: "Saisir un nom, un cépage, une région ou un producteur"}

.index-items
  - @wines.sort.to_h.each do |letter, wines|
    %h2= letter
    - wines.each do |wine|
      .index-item
        %h4= link_to wine.name, edit_admin_wine_path(wine.id)
